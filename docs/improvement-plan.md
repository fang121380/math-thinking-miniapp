# 梵数学改进计划

## 第一批：工程基础和已复现缺陷

- 将展开后的源码、测试、题库审计和 GitHub Actions 工作流纳入版本管理。
- 统一本地与 CI 的 Node.js 24 环境及 `prepare:upload` 检查入口。
- 使用公共配置模板和可重复执行的初始化命令，保留本机 AppID。
- 修复掌握度 0 被当成默认值 50 的选题错误，增加行为回归测试。
- 每批完成后先测试、审计，再提交；本地提交不等于已推送或云端 CI 已通过。

## 完成后的 GitHub 同步要求

用户要求：**全部做完并测试通过后，必须同步到 GitHub，不能只留在本地。**

- 每批改动通过验证后及时 commit；全部约定工作完成后，对最终代码执行 `npm run prepare:upload` 和适用的运行验证。
- 同步目标为 [fang121380/math-thinking-miniapp](https://github.com/fang121380/math-thinking-miniapp) 的对应开发分支。同步前核对远程历史，保留既有提交，正常 push，不以覆盖远程历史的方式处理差异。
- 推送后确认远程分支包含本地提交，并检查 GitHub Actions。失败时修复并重新测试、提交和同步。
- 完成汇报中列出测试结果、commit、GitHub 分支链接和 CI 状态；未验证的真机项目与同步阻塞必须如实列出。
- 该同步已获用户授权，无需在正常收尾时重复询问是否推送。若缺少提交身份或仓库访问权限，应明确说明尚未同步及所缺信息。

## 后续批次与验收依据

已开展人教版四年级的首轮范围核对与内容修复，见[校准记录](qa/2026-09-17-rjb-grade-4-review.md)。这不代表整学年教材校准、教师审核或真机验证已经完成。

1. **教材校准**：先以默认人教版四年级为试点，核对有明确年份的教材目录、上下册与知识点映射。保留逐项来源和未覆盖项，不把自动检查标为人工教研结论。
2. **题库教研**：明确模板族、变式、审核状态和实际审核记录；为代表题准备人工复核清单。题量、去重数量与教学有效性分别报告。
3. **自适应策略**：在现有难度选择契约上设计可解释的难度调整，覆盖连续错误、提示依赖、冷启动和知识点隔离。先做离线回放，教学效果另由真实试用验证。
4. **数据可靠性**：先设计可导出、可校验、可恢复的本地备份；多孩子档案与云同步需要另行确定账号、后端和冲突处理方案。
5. **内容更新**：确定远程内容数据格式、校验与兼容规则、原子切换和失败回退，再接入实际发布端点。当前仅有更新通知预留接口。
6. **运行验证**：在微信开发者工具和真机验证诊断、练习、错题补救、游戏、音频、返回路径、包体积与加载表现；记录设备和结果。

当前自动测试和审计不替代教材人工审核、学生试用或真机验收。

## 续跑记录（2026-09-18）

- 数据可靠性批次已完成：`miniprogram/utils/progress-backup.js` 定义版本化 JSON 备份、稳定序列化和校验值；“我的”页使用剪贴板完成复制备份与粘贴恢复，恢复前二次确认，恢复后走现有进度规范化。
- 验收：备份往返、空数据、非法 JSON、版本不兼容、校验失败和字段顺序稳定性测试通过；`npm run prepare:upload` 通过，审计 21,261 道题无错误，Node 测试 435 项通过。
- 自适应离线回放批次已完成：`applyAdaptiveOutcome` 和 `replayAdaptiveSequence` 统一答题状态更新；覆盖连续错误降级、提示依赖、冷启动默认掌握度和知识点隔离，答题页已复用该入口。`npm run prepare:upload` 通过，审计 21,261 道题无错误，Node 测试 437 项通过。
- 内容更新协议批次已完成：`content-update.js` 增加带校验的题包格式、应用版本兼容、题面/ID 唯一性检查，以及 active/previous 原子安装和内置题库回退；只提供本地可测试接口，没有虚构线上发布端点。`npm run prepare:upload` 通过，审计 21,261 道题无错误，Node 测试 440 项通过。
- GitHub 已同步：`codex/development-foundation` 本次模拟器验证前远程提交 `461f92f`，Verify 运行 `35299723676` 通过；远程 `main` 未被覆盖，保留其上游提交。
- 微信开发者工具端口阻塞已解除，模拟器完成四年级摸底、错题补救完整返回链路、自主练习入口、小学游戏及初中分包任务加载/返回；备份恢复采用模拟剪贴板及确认结果。详见[运行记录](qa/2026-09-18-devtools-smoke.md)。440 项自动测试及题库审计再次通过。
- 下一步优先手机实测音频、剪贴板授权、长页滚动及游戏完整结算；模拟器未覆盖所有游戏或全部教材范围。云同步和线上内容发布仍没有实际账号、后端或端点配置，不作可用服务声明。
- 新增答题会话保护：重复点击不会重复计分或更新掌握度；跨日和题组替换会安全回到当前练习；本地导入进度的集合、计数、能力值和弱项字段会先归一化；设置页切换音效立即同步到当前页面。实现提交 `eae6ac6` 已推送，`npm run prepare:upload` 通过，447 项测试，21,261 道题审计无错误，GitHub Actions Verify 运行 `35323334552` 通过。
- 题库质量批次已完成：人教版四年级上册三个核心知识点各新增 12 道代表题，共 36 道；统一记录任务类型、表征方式、思维层级和易错概念，并让挑战模式和题库审计使用这些字段。题库版本升至 `2026.09.18.1`。实现提交 `1c4d8f9` 已同步，`npm run prepare:upload` 通过，448 项测试，21,297 道题审计无错误，GitHub Actions Verify 运行 `35332294129` 通过；详见 `qa/2026-09-18-question-bank-quality.md`。下一步仍需教师逐题复核、手机实测和扩大教材范围。

## 续跑记录（2026-09-20）

- 根据手机截图修复两类小学游戏：目标数改为检查托盘中完整且数学上成立的实际算式，不再只接受预设令牌顺序；隔项数列会明示奇数位、偶数位的观察方法和分别增量。
- 旧缓存的含糊隔项数列会被挑战质量校验判无效并重新生成；回归用例锁定 `36-17+16=35` 与 `22、26、36、46→50` 两个具体场景。
- 题库内 36 道四年级思维题的公共课程元数据已统一生成，题面、答案、提示和解析保持不变；用于抵消新判定逻辑的主包增量。
- `npm run prepare:upload` 通过：21,297 道题审计 0 错误，451 项测试全通过，主包 1,834,093 bytes。详见[2026-09-20 游戏修复记录](qa/2026-09-20-game-correction.md)。
- 实现提交 `446ada9` 已推送，GitHub Actions Verify 运行 `35485105803` 通过。手机真机点击和结算仍需重新编译后复验。
- 手机复验暴露首批回归未覆盖页面提交分支：数列题数据未标记 `choice`，导致选中正确答案后检查无动作。现已补齐生成模式，使页面依据实际渲染模式判定，并让旧缓存题自动换新。
- 提示会直接列出第 1/3/5 和第 2/4 个数的具体数值及步长；答对后的选项使用绿色成功状态。页面级回归和开发者工具精确场景均已通过。
- 最新验证：21,297 道题审计 0 错误，455 项测试通过，主包 1,834,480 bytes；开发者工具完成 `18、34、30、44→42` 的提示、选择、检查和奖励，无 JS 异常。
- 追加修复提交 `d8e0c95` 已推送，GitHub Actions Verify 运行 `35486440799` 通过。
- 微信代码质量体积修复已完成：10 首可选 BGM 改用已配置域名上的 Wikimedia MP3 转码，包内交互短音效保留；测试门槛同步为主包 1.5 MiB、全包媒体 200 KiB，并覆盖微信检查器使用的 SVG/音视频扩展名。
- `npm run prepare:upload` 通过：21,297 道题审计 0 错误，455 项测试通过。开发者工具 CLI 预览主包 682,517 bytes、总包 729,510 bytes，源码媒体 169,154 bytes；10 首曲目均在模拟器进入可播放状态。
- 实现提交 `6c3b9d4` 已推送，GitHub Actions Verify 运行 `35488581806` 通过。详见[2026-09-20 微信代码质量体积修复](qa/2026-09-20-package-quality.md)。质量面板需手动“重新扫描”刷新旧结果，真机弱网/断网 BGM 行为仍待验收。
- iOS 真机调试工具链已修复：补齐开发者工具内置 `ios_webkit_debug_proxy` 所需的 Homebrew 动态库后，代理按工具原参数稳定监听 `29374`；清除失效系统代理地址并重启后，项目可正常打开和编译。当前未检测到连接的 iPhone，尚待 USB 连接、信任设备及手机侧调试复验，详见[开发者工具运行记录](qa/2026-09-18-devtools-smoke.md)。
- 四年级题库精度批次已完成：修正两道整百估算的错误答案，其中 `398×21` 原选项无正确答案；把两道伪逆向题改为真实反推，消除一处估算错误分析和一处验算选项歧义。审计器现在会从直接估算题干提取算式，并识别带空格或千位分隔符的数字答案。
- 题库版本升至 `2026.09.20.1`；`npm run prepare:upload` 通过，21,297 道题审计 0 错误，458 项测试通过。实现提交 `244386c`；详见[精度修复记录](qa/2026-09-20-question-bank-precision.md)。
- 已生成 36 道代表题的[人工复核队列](qa/2026-09-20-grade4-thinking-review-queue.md)，当前全部待复核、已确认 0 道。下一步需要教师或教研人员逐题确认，不能把自动审计标成已完成人工审核。
- 实现提交 `244386c`、记录提交 `b73d4a5` 已推送到 `codex/development-foundation`；GitHub Actions Verify 运行 `35499480476` 通过。
- 题库第二批可读性修复已完成：三年级分数/质量单位与五年级因数倍数/分数加减/单位换算共 10 道基础题不再使用空泛提示，全部补充具体操作和两步解析。题库版本升至 `2026.09.20.2`；`npm run prepare:upload` 通过，21,297 道题审计 0 错误，459 项测试通过。
- 题库第三批可读性修复已完成：一年级整时/图形/长度比较和二年级数位/长度单位/直角共 11 道题改为可执行观察提示与两步解析。题库版本升至 `2026.09.20.3`；`npm run prepare:upload` 通过，21,297 道题审计 0 错误，460 项测试通过。
- 题库第四批可读性修复已完成：六年级分数乘除法、圆、负数共 8 道题补充约分、倒数、半径直径和数轴比较步骤。题库版本升至 `2026.09.20.4`；完整验收待本批提交后执行。
- 追加修复一年级长度减法题的空泛提示；截至 `2026.09.21.1`，一至六年级公开基础题已无“想一想定义”提示。
- 修正 14 道概念填空题被错误标成“解决问题”的交互问题，并新增门禁；`npm run prepare:upload` 通过，21,297 道题审计 0 错误，462 项测试通过。

## 续跑记录（2026-09-22）

- 四年级 36 道代表题完成新一轮项目内容复核：估算式、除法逆推的近似结论、简答判分要求及验算干扰项已校正；版本 `2026.09.22.1`。人工复核队列已更新，36 道仍待教师或教研人员确认（已确认 0 道），自动审计不算教学效果验证。
- `npm run prepare:upload` 通过：21,297 道题审计 0 错误，463 项测试全通过。实现提交 `87a2259`；仍需手机实测和真实教师复核。
