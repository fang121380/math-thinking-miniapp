# 微信开发者工具运行检查（2026-09-18）

本次验证应用代码 `461f92f`，未修改应用实现。开发者工具服务端口已开启，CLI `islogin` 返回登录成功，`auto` 成功打开项目并建立自动化连接。原服务端口阻塞已解除，无需再次让用户找设置。

## 环境与数据保护

- macOS 微信开发者工具 Stable 2.02.2608070，基础库 3.17.3。
- 模拟器 iPhone 12/13 (Pro)，逻辑尺寸 390 × 844；不是真机。
- 自动化工具安装在临时目录，未新增项目依赖。
- 测试前保存原进度，以临时空白档案进行测试；finally 恢复原进度并比较一致，返回首次进入页。
- 备份验证模拟 `setClipboardData`、`getClipboardData` 和确认弹窗结果，结束恢复 API；未覆盖用户系统剪贴板，也未验证原生授权弹窗。
- 不提交本机 AppID、进度快照、自动化日志或令牌。

## 已通过的场景

1. 首次进入选择小学、四年级，进入五道摸底题。通过按钮和输入组件作答，包含一道错题及一道使用提示的题，跳转结果页。
2. 从结果页进入今日训练，选择挑战，生成题组并打开第一题。
3. 故意答错后进入错题分析，完成小台阶题和同类再试，返回原今日题组第 2 题。
4. “我的”页复制备份，再修改测试星数并粘贴恢复，确认存储星数恢复到导出时的值（剪贴板和确认弹窗模拟）。
5. 从“我的”进入自主练习，点击开始，打开自主练习题。
6. 小学游戏推荐入口打开计算闯关，页面加载无错误，返回游戏列表。
7. 切换初中，打开推荐方程实验室分包页面，页面加载无错误，返回任务列表。

成功运行未收到自动化通道的 JS exception 事件。保存并查看错题分析、计算闯关和方程实验室截图，核心内容可见。先前脚本因读到切换前页面及剪贴板模拟返回方式不正确而失败；调整测试等待与模拟结果后全部上述场景通过，未据此改动应用代码。

`npm run prepare:upload`：440 项测试全部通过；21,261 道题审计 0 错误。

## 未验收范围与下一步

- 手机微信中的真实音效/BGM、后台恢复、剪贴板授权、跨设备粘贴、长页滚动与点击体验。
- 今日练习全题完成结算、所有游戏完整通关、所有教材/年级/学期组合、成长页完整交互。
- 实际上传包大小和手机分包下载表现；源码体积检查已由现有测试覆盖，不等于上传包测量。
- 教师审核、学生试用、全学年教材校准、实际云同步及线上题包发布。

后续先检查最新 Git 日志与本记录，只补未覆盖场景；不重复服务端口排障，不将模拟器通过写成真机通过。GitHub 同步与 CI 的最新状态以本次文档提交及对应 Actions 运行为准。
