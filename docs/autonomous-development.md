# 自动续跑任务

## 设置建议

在桌面应用的定时任务（Scheduled / Automations）中创建任务，名称为“梵数学持续完善”。选择本地项目 `/Users/andrew/Documents/ChatGPT/梵数学`，建议每小时运行一次；若界面支持在现有会话中调度，优先沿用当前会话。只启用一个开发任务，避免同时修改同一工作区。

本地执行需要电脑开机并保持唤醒、应用运行、项目目录可访问。定时唤起不是无限连续执行，运行仍可能受网络、额度、登录或权限影响。

当前对话未提供创建定时任务的工具；本文件只准备续跑指令，不表示任务已保存或启用。请在应用中保存后确认下一次运行时间。

官方说明：<https://learn.chatgpt.com/docs/automations>。

## 可复制的任务指令

```text
继续完善本地项目 /Users/andrew/Documents/ChatGPT/梵数学。

每次先读 CONTRIBUTING.md、docs/improvement-plan.md、docs/autonomous-development.md 和最近的 Git 日志、工作区状态。接着推进尚未完成的工作，不重复已经通过验证的批次。不要只给建议或等待“继续”。有其他开发运行正在修改同一目录时不要并行编辑。

依次推进：教材与题库质量、自适应策略、本地数据备份恢复、内容更新机制、微信运行验证。每批选择有明确验收标准、能独立完成的改动，实现后运行必要测试；每次实现 commit 前运行 npm run prepare:upload，失败就修复再测，通过后及时 commit。把完成项、测试结果、后续工作和真实阻塞写进本文件。不要更改用户正在编辑的无关文件，不要提交本机 AppID、私有配置、令牌或日志。

教材校准只使用可追溯的出版社/官方资料，注明版本；不能把代码审计、模型复核当成教师审核或学生效果验证。自适应先保留已有手选难度契约并加入可验证的策略；云同步和在线内容发布在缺少实际服务配置时先完成可测试的本地接口与恢复机制，不编造可用服务。

遇到一个阻塞先推进其他独立工作。只有必须由用户处理的登录、权限、关键产品选择或无法解决的问题才通知用户；相同阻塞没有变化时保持安静，不反复问相同问题。不要关闭安全机制或假称完成无法执行的验证。

全部约定改动完成并通过适用测试后，同步到 GitHub fang121380/math-thinking-miniapp 对应开发分支，确认远程 commit 与 GitHub Actions 结果。用户已授权正常推送。先取回并核对远程历史：本地源于 ZIP 的初始提交与上游历史独立，当前开发分支单独推送，尚未与 main 整合；若后续整合，必须保留上游内容，禁止强推覆盖远程历史。没有完成同步、CI 或真机验证时如实记录。

在有实质里程碑、失败需要处理、需要用户动作或全部完成时通知；普通无变化运行保持安静。全部完成后停止新增改动；如可用则暂停此定时任务，否则提示用户暂停。
```

## 当前进度（2026-09-18）

- 分支：`codex/development-foundation`。
- 已完成工程初始化与 CI 准备：`ce5e9cf`。在干净检出目录验证了初始化及 421 项测试。
- 已修复掌握度 0 的排序：`7643f9c`，回归测试先失败后通过。
- 已完成四年级首轮内容修复与课程来源标记：`be37931`，详见 `docs/qa/2026-09-17-rjb-grade-4-review.md`。
- 最终实现检查：431 项测试通过，21,261 道题审计 0 错误。题库版本 `2026.09.17.1`。
- Git 身份已在本仓库配置为已登录 GitHub 账号 `fang121380`，使用该账号的 GitHub noreply 邮箱。`gh api` 已确认对目标仓库有写入权限。
- `origin` 为 `https://github.com/fang121380/math-thinking-miniapp.git`；开发分支已推送，历史同步详情见下方。
- 微信开发者工具服务端口已开启，CLI 登录和自动化连接成功；模拟器冒烟通过，真机尚未验收。
- 工作区出现未跟踪的 `.codex/`，尚未审查，不能随手纳入提交。
- 已完成本地数据备份恢复批次：新增带版本和 FNV-1a 校验的进度备份格式，在“我的”页提供复制备份、粘贴恢复，恢复沿用进度规范化；新增备份往返、篡改、版本和页面契约测试。
- 本批验证：`npm run prepare:upload` 通过，题库审计 21,261 道题无错误，Node 测试 435 项通过。
- 已完成自适应离线回放批次：新增 `applyAdaptiveOutcome` 与 `replayAdaptiveSequence`，答题页复用统一状态归并；提示后答对仍进入薄弱点，连续错误只降低对应知识点，冷启动掌握度仍按 50 处理，新增隔离回放测试。
- 本批验证：`npm run prepare:upload` 通过，题库审计 21,261 道题无错误，Node 测试 437 项通过。
- 已完成内容更新协议批次：新增版本化题包、稳定校验、兼容性/唯一题面校验，以及 active/previous 原子安装和内置题库回退接口；未接入不存在的线上端点。
- 本批验证：`npm run prepare:upload` 通过，题库审计 21,261 道题无错误，Node 测试 440 项通过。
- GitHub 同步：远程 `main` 保留原上游历史；已正常推送 `codex/development-foundation`，本次验证前远程提交为 `461f92f`。GitHub Actions Verify 运行 `35299723676` 通过（`prepare:upload` 全量通过）；仅有 runner 的 Node.js 20 动作弃用提示。
- 2026-09-18 微信服务端口阻塞已解除；已完成四年级摸底、今日错题分析、小台阶→同类再试→返回原题序、自主练习入口、小学游戏及初中分包任务加载和返回验证。备份恢复使用模拟剪贴板与确认结果，未覆盖系统剪贴板授权。原学习记录已恢复。详见 `docs/qa/2026-09-18-devtools-smoke.md`。
- 本批 `npm run prepare:upload` 再次通过：440 项测试，21,261 道题审计无错误。未修改应用实现；本批只记录运行验收。
- 下一步：手机实测（音频、系统剪贴板授权、长页操作、游戏完整结算及加载体验）。完整教材覆盖、教师审核、学生试用、实际云服务和远程内容发布仍未完成；不要重复要求用户开启已开启的服务端口。
- 已完成答题会话可靠性批次：答题页绑定题组和题目身份，防止快速重复提交、跨日旧题提交及自主练习换题后的旧页面提交；保存失败仍可重试。进度保存补充集合、计数、能力值和弱项字段归一化；“我的”页切换音效后立即同步运行状态。新增 7 项回归测试。
- 本批验证：`npm run prepare:upload` 通过，447 项测试，21,261 道题审计无错误；开发者工具冒烟场景全部通过且无 JS exception。实现提交 `eae6ac6` 已推送，GitHub Actions Verify 运行 `35323334552` 通过。
- 已完成四年级上册题库质量批次：为人教版四年级上册三个核心知识点加入 36 道代表题，补充任务类型、表征方式、思维层级和易错概念元数据；挑战模式可识别这些任务，估算目标和方框逆向题纳入自动审计。详见 `docs/qa/2026-09-18-question-bank-quality.md`。
- 本批验证：`npm run prepare:upload` 通过，448 项测试，21,297 道题审计无错误；主包体积 1,834,742 bytes，低于 1.75 MiB 阈值。实现提交 `1c4d8f9` 已同步，GitHub Actions Verify 运行 `35332294129` 通过；仅有 Actions Node.js 20 弃用及未来 runner 镜像迁移提示。

## 续跑记录（2026-09-20）

- 已修复“目标数挑战”将其他合法令牌顺序判错的问题；现在依据玩家实际摆出的完整算式判定，`36-17+16=35` 可正确通过。
- 已修复“规律侦探”四年级数值放大后规则说明丢失的问题；`22、26、36、46、?` 现会明确指引分开观察奇数位和偶数位，答案为 50。旧缓存中说明含糊的隔项数列会被自动替换。
- 新增针对截图场景的回归测试，并将 36 道思维题的重复课程元数据收口到统一生成函数以控制主包体积。详见 `docs/qa/2026-09-20-game-correction.md`。
- 本地验证：`npm run prepare:upload` 通过，451 项测试，21,297 道题审计无错误；主包 1,834,093 bytes，低于 1.75 MiB 阈值。
- 实现提交 `446ada9` 已推送，GitHub Actions Verify 运行 `35485105803` 通过。手机端仍需在重新编译后完成两种游戏的点击与结算复验。
- 手机复验发现数列选择后无法完成：根因是生成题缺少 `choice` 模式，页面只在渲染时临时补齐，提交时却根据原题模式分支。已同时修正生成契约、页面判定和旧缓存校验，并新增直接调用页面检查方法的回归用例。
- 隔项提示改为列出“第1、3、5个数”和“第2、4个数”的具体数字及步长；选中未提交为中性黄色，答对完成后为绿色。
- 追加验证：`npm run prepare:upload` 通过，455 项测试，21,297 道题审计无错误，主包 1,834,480 bytes。开发者工具重现 `18、34、30、44→42`，提示、检查、完成奖励和绿色答对状态均通过，无 JS 异常，原进度已恢复。
- 追加修复提交 `d8e0c95` 已推送，GitHub Actions Verify 运行 `35486440799` 通过。
- 已修复微信代码质量的两个体积未通过项：保留 10 首可选背景音乐，改为从已配置的 Wikimedia 下载域名流式播放，并移除包内 948,950 bytes 的背景音乐副本；低延迟交互音效继续离线提供。
- 体积测试已与微信 Stable 2.02.2608070 内置检查器对齐：主包小于 1.5 MiB，整个编译包的图片、SVG、音频和视频总量小于 200 KiB。源码口径主包 886,133 bytes、媒体 169,154 bytes；CLI 实际预览主包 682,517 bytes、总包 729,510 bytes。
- 10 首远程 MP3 均在微信模拟器触发 `onCanplay`；`npm run prepare:upload` 通过，455 项测试、21,297 道题审计 0 错误。实现提交 `6c3b9d4` 已推送，GitHub Actions Verify 运行 `35488581806` 通过。详见 `docs/qa/2026-09-20-package-quality.md`。
- 代码质量面板会保留旧扫描结果，需点击“重新扫描”刷新。真机下一步重点验证弱网/断网时可选背景音乐的提示与恢复；本地答题音效不受网络影响。
- 已定位并修复 macOS 上 iOS“真机调试”按钮无反馈：开发者工具内置代理因缺少 Homebrew 的 `libimobiledevice`、`libplist`、`libusbmuxd` 和 OpenSSL 动态库而秒退；依赖补齐后，代理已按原参数稳定监听 `29374`，项目重新打开并编译成功。另清除了已关闭但仍残留地址的失效系统代理。详见 `docs/qa/2026-09-18-devtools-smoke.md`。
- 当前 USB 枚举未发现 iPhone，下一步只需连接并解锁手机、信任此 Mac 后再次点击“真机调试”，再记录设备侧结果；不要重复安装依赖或排查按钮事件。
